#include "catch_main.h"

TEST_CASE( "Displayer", "" ) {
    // Displayer ds;

    // ds.append_pointer( true, "yo", [&]() { ds << 19; } );

    // Str res;
    // ds.write_to( res, { .ensure_endline = true } );
    // std::cout << res;
    P( 17, 987 );
}
