from distutils.core import setup
import setuptools
packages = ['progbar']
setup(name='txdprogbar',
    version='1.0.5',
    author='唐旭东',
    packages=packages,
    package_dir={'requests': 'requests'})