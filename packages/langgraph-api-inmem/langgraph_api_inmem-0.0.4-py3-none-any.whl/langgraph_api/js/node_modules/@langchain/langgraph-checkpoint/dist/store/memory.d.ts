import { BaseStore, type OperationResults, type Operation } from "./base.js";
export declare class MemoryStore extends BaseStore {
    private data;
    batch<Op extends Operation[]>(operations: Op): Promise<OperationResults<Op>>;
    private getOperation;
    private searchOperation;
    private putOperation;
    private listNamespacesOperation;
    private doesMatch;
}
