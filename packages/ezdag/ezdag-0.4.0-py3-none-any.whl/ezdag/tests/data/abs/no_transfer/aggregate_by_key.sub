universe = vanilla
executable = /path/to/aggregate_by_key
arguments = $(aggregate) $(data)
request_cpus = 1
request_memory = 2000
output = $(log_dir)/$(nodename)-$(cluster)-$(process).out
error = $(log_dir)/$(nodename)-$(cluster)-$(process).err
notification = never

queue