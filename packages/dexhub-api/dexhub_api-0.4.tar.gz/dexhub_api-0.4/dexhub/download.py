from typing import Dict

def my_dataset(project: str = None) -> Dict:
    """
    Get the dataset for the specified project.
    
    Args:
        project (str): the name of the project to get the dataset for.
    
    Returns:
        dict: the dataset for the specified project.
    """
    
    return True 

def global_dataset() -> Dict:
    """
    Get the entire global dataset.
    
    Returns:
        dict: the entire global dataset.
    """
    return True
