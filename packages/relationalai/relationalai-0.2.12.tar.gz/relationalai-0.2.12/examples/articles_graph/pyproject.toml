[project]
name = 'Articles Graph Project'
version = '0.1.0'
description = 'RelationalAI Library and CLI'
readme="examples/articles_graph/README.md"
authors = [
    { name="RelationalAI", email="support@relational.ai" },
]
requires-python = ">= 3.10"
dependencies = [
    "spacy>=3.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
