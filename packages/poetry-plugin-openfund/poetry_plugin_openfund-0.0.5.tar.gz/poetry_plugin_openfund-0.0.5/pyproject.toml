[tool.poetry]
name = "poetry-plugin-openfund"
version = "0.0.5"
description = "Poetry plugin that updates dependencies and bumps their versions in pyproject.toml file"
authors = ["Mousa Zeid Baker"]
packages = [
    { include = "poetry_plugin_openfund", from = "src" },
]
license = "MIT"
readme = "README.md"

keywords=[
    "packaging",
    "dependency",
    "poetry",
    "update",
    "upgrade",
]
classifiers=[
    "Development Status :: 5 - Production/Stable",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[tool.poetry.dependencies]
python = "^3.8"
poetry = "^1.8.4"

[tool.poetry.group.dev.dependencies]
pre-commit = "^2.21.0"
pytest = "^7.4.4"
pytest-mock = "^3.14.0"

[tool.poetry.plugins."poetry.application.plugin"]
up = "poetry_plugin_openfund.plugin:OpenfundApplicationPlugin"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.black]
line-length = 80

[tool.isort]
profile = "black"
