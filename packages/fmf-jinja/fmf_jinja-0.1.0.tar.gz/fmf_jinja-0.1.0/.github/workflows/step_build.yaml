on:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    name: 🐍 sdist/wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: hynek/build-and-inspect-python-package@v2
